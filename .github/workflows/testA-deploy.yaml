name: Deploy Staging TestA
on:
  push:
    branches:
      - master
      - staging

jobs:
  deploy:
    permissions: write-all
    uses: ./.github/workflows/deploy-workflow.yaml
    with:
      app-path: ./test-a
      stage: staging
    secrets: inherit

