name: Deploy Staging TestA
on:
  push:
    branches:
      - master
      - staging

jobs:
  deploy:
    permissions:
      id-token: write-all
      contents: read
    uses: ./.github/workflows/deploy-workflow.yaml
    with:
      app-path: ./test-a
      stage: staging
    secrets: inherit

