name: Deploy Staging TestA
on:
  push:
    branches:
      - master
      - staging

jobs:
  deploy:
    uses: ./.github/workflows/deploy-workflow.yaml
    with:
      app-path: ./test-a
      stage: staging
